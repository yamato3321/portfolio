<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>プロフィール設定</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" th:href="@{/css/style.css}">
	<link rel="stylesheet" th:href="@{/css/profile.css}">
</head>
<body>
    <h1>プロフィール編集</h1>
    
    <form th:action="@{/profile}" th:object="${editProfileForm}" method="post">
        <label>メールアドレス（変更不可）:</label>
        <input type="email" th:field="*{email}" readonly /><br><br>

        <label>名前:</label>
        <input type="text" th:field="*{name}" required /><br><br>
		
		<label>現在のパスワード:</label>
		<input type="password" th:field="*{currentPassword}" required 　novalidate/><br><br>
		<div th:if="${passwordError}">
		    <p style="color:red;" th:text="${passwordError}"></p>
		</div>


		<label>新しいパスワード:</label>
		<input type="password" th:field="*{newPassword}" required　novalidate/><br><br>

        <button type="submit">更新</button>
		
		<a th:href="@{/tasks}">戻る</a>
    </form>

    <div th:if="${param.updated}">
        <p style="color:green;">プロフィールを更新しました！</p>
    </div>

   
</body>
</html>
